{
  "name": "@vertex/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "vertex": "./bin/vertex.js"
  },
  "scripts": {
    "build": "pnpm --filter @vertex/core build && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "pnpm --filter @vertex/core build && vitest run"
  },
  "dependencies": {
    "@vertex/core": "workspace:*",
    "playwright": "^1.63.0"
  }
}
